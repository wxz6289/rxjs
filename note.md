# Rx.js

## Observable

拉取和推送是两种不同的协议，描述了数据生产者如何与数据消费者进行通信。

函数调用
订阅类似于函数调用，并非异步。

可观察对象可能同步或异步传递值。

EventEmitter 共享副作用，无论是否存在订阅者都会立即执行
Observable不会立即执行,并且是惰性的，不共享副作用.

使用observable.subscribe()指定的观察者不会在可观察对象中注册为监听器。可观察对象甚至不会维护已连接的观察者列表。调用subscribe只是一种启动“可观察执行”并向该执行的观察者传递值或事件的方式。
